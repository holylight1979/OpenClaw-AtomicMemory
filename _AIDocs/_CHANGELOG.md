# _AIDocs Changelog

| 日期 | 變更 | 影響文件 |
|------|------|---------|
| 2026-03-18 | **Owner Challenge Auth 修正 + 使用者記憶刪除權限放寬**: (1) Fix Escalation 調查：確認根因為 jiti cache 陳舊（plugin .ts 修改後未清 cache 導致舊編譯結果持續生效）; (2) challenge flow 重排修正保留：challenge 在 command interception 之前執行 + authIntentHandled flag 豁免; (3) Ollama 移除：auth intent 偵測不再依賴 qwen3（純 keyword regex）; (4) 使用者記憶刪除權限：移除 [臨] confidence 限制，改為 sources.senderId 比對（使用者可刪除自己建立的任何記憶）; (5) run.ts hookCtx 已有 sender fields（lines 331-335），dist/ 已包含; (6) Discord owner 認證 E2E 驗證通過（senderId=831783571513278464, hex challenge + 成功註冊） | index.ts, _CHANGELOG.md |
| 2026-03-16 | **Owner 認證機制 + MEMORY.md 動態索引**: (1) owner-challenge.ts 新建 — hex challenge 生成/MD5 驗證/24hr cooldown/Ollama 90% 意圖確認; (2) memory-index.ts 新建 — MEMORY.md 程式碼自動管理，+4/-1 scoring（cap 20），每次對話更新，score=0 移除; (3) permission-guard.ts 新增 registerOwnerPlatform() + authenticatedAt; (4) index.ts: challenge 流程接線（before_agent_start 意圖偵測→challenge→驗證→註冊）+ MEMORY.md scoring 獨立於 capture（agent_end 頂部）; (5) E2E 驗證：MEMORY.md scoring 累加正常（4→8）、新 atom 正確加入、workspace 碎片已排除 | owner-challenge.ts, memory-index.ts, permission-guard.ts, index.ts |
| 2026-03-15 | **記憶+身份統一整合**: (1) System.Owner.json 集中式身份 Registry — 統一 owner/bot/admin 跨平台身份定義（取代 6+ 散布位置），permission-guard.ts 平台感知 owner 匹配 + 自動 fallback; (2) Recall 統一搜尋 Phase 11 — recall-engine.ts 加入 workspace 日期檔 keyword 搜尋，workspace facts 包裝為 pseudo-atom（score×0.35 低權重）混合排序，去重避免與既有 atom 重複; (3) /whoami 指令 — before_agent_start 偵測 whoami 意圖回傳 senderId+channel+permission; (4) Workspace 增強 — SOUL.md 加入讀取清單 + context-formatter source 標記; (5) config.ts systemIdentityPath 設定 | permission-guard.ts, recall-engine.ts, workspace-reader.ts, context-formatter.ts, types.ts, config.ts, index.ts, System.Owner.json |
| 2026-03-15 | **atomic-memory G-INT**: 三目標整合驗證 — 靜態驗證（TSC 零錯誤、hook 順序、config 三區段共存、無 runtime 循環依賴）; 整合測試 33 cases 全 PASS（5 場景：跨群組 scope routing + 跨平台 identityLinks entity resolution + 權限三級攔截 + G1+G2+G3 交叉 + bot self-awareness）; live gateway 測試（store/recall/self-awareness 全通過）; 衝突檢查（crossPlatform vs memoryIsolation 獨立、scope vs sources 正交、defaults 零設定可用） | Extensions.md |
| 2026-03-15 | **atomic-memory G2-B**: 跨頻道 Recall API + 查詢工具 — atom_recall 新增 crossPlatform 參數（啟用時 isolationMode 切 shared）; atom_whois 新增（跨平台人物查詢, 彙整所有 person atom + related, 含 owner-only 權限檢查）; auto-recall hook 自動偵測 info-request+人名→啟用 crossPlatform; entity merge suggestion（nameSimilarity ≥ 0.7 偵測跨平台同人→注入確認提示）; context-formatter 加 platform: tag + 跨平台來源提及指引; store.list("person") 呼叫合併優化 | Extensions.md |
| 2026-03-15 | **atomic-memory G1-B**: 跨群組命令廣播 + 記憶傳播 — types.ts 新增 AtomScope(global/user/group) + scope 欄位 + memory-store intent + lastIntent; scope-classifier.ts 新建命令路由（memory-store→user, command→group, personal facts→user, default→global）; intent-classifier.ts 新增 memory-store intent（記住/以後都/我叫/my name is）; atom-parser.ts 解析 Scope metadata（舊 project→global 向下相容）; atom-writer.ts 寫入實際 scope; recall-engine.ts 三層 scope 過濾（user→senderId, group→channel, global→memoryIsolation）+ recallScopes 參數; atom-store.ts findOrCreate 接受 scope; entity-resolver.ts person atom 預設 user scope; index.ts recall 加 getRecallScopes(intent), capture 加 classifyScope(lastIntent, factText), atom_store tool 加 scope | Extensions.md |
| 2026-03-15 | **atomic-memory G2-A**: 跨平台人事時地物 Identity + 實體合併 — Core: PluginHookAgentContext 新增 identityLinks 欄位 + attempt.ts 從 config.session 傳入; entity-resolver.ts V2: resolveEntity 新增 identityLinks canonical match (Strategy 2), ensurePersonAtom 支援跨平台 source autoMerge, 新增 linkPersonAcrossPlatforms + resolveLinkedPeerIds, 移除 substring matching false positive; recall-engine.ts: user-scoped isolation 改為 identity-aware (linked peers 視為同人), 新增 crossPlatformRecall boost; atom-store.ts: update() patch 支援 triggers; config.ts 新增 crossPlatform 區段; index.ts: 新增 atom_link tool, recall/ensurePersonAtom 傳入 identityLinks | Extensions.md, Channels-Routing.md |
| 2026-03-15 | **atomic-memory G3-B**: Bot 自我認知 + 指令攔截 — permission-guard.ts 新建（三級權限 owner/admin/user、設定指令偵測、動態身份 prompt、capability context、runtime admin 持久化）; config.ts 新增 botName/adminIds; index.ts: before_prompt_build 升級為 buildSelfAwarenessPrompt()、before_agent_start 加指令攔截+自我認知查詢; atom_store/atom_forget/atom_link 升級為 owner+admin 雙角色; atom_forget 分級刪除（regular user 只能刪自己的[臨]）; 新增 atom_permission 工具（add/remove/list admin）; handleForgetFlowAsync 加 tiered check + candidate list 過濾（反思修正 Critical 漏洞）; openclaw.plugin.json 補 permission+crossPlatform schema | Extensions.md |
| 2026-03-15 | **atomic-memory G3-A**: 權限者認知（Permission + ACL）— config.ts 新增 permission 區段（ownerName/toolWriteRequiresOwner/botSelfAwareness）; index.ts atom_store/atom_forget 加入 senderIsOwner 權限檢查; before_prompt_build hook 注入自我認知 prompt（appendSystemContext cached） | Extensions.md |
| 2026-03-15 | **atomic-memory S8**: Integration Test + Polish — tsc --noEmit 零錯誤; 40 個 import/export 全部驗證通過; 4 個 hook 呼叫鏈完整（session_start/before_agent_start/agent_end/session_end）; openclaw.plugin.json configSchema 補齊 tokenBudget/actr/memoryIsolation/episodic/wisdom/selfIteration 6 個區段; 無 TODO/FIXME/HACK; 安全檢查通過 | Extensions.md |
| 2026-03-15 | **atomic-memory S6**: Self-Iteration Engine — self-iteration.ts（detectOscillation: 掃描近 N 個 episodic atomsModified 計算跨 session 重複修改; checkPeriodicReview/saveReviewMarker: 定期回顧提醒+marker 持久化; calculateMaturity: learning/stable/mature 三階段; loadIterationState/saveIterationState: _iteration/state.json）; config.ts 新增 selfIteration 區塊（enabled/oscillationWindow/oscillationThreshold/reviewInterval）; index.ts session_start hook 整合 periodic review + maturity logging, session_end hook 整合 oscillation detection + state update | Extensions.md |
| 2026-03-15 | **atomic-memory S4**: Episodic Engine — episodic-engine.ts（generateEpisodicSummary: rule-based 從 SessionState 萃取摘要; storeEpisodicAtom: MD 寫入 episodic/ 含 Type:episodic/TTL:24d; cleanExpiredEpisodic: TTL 過期自動刪除; listEpisodicSummaries: 讀取+反解析）; config.ts 新增 episodic 區塊（enabled/minDurationMs/minTurns/ttlDays）; index.ts session_end hook 整合 | Extensions.md |
| 2026-03-15 | **atomic-memory S7**: Wisdom Engine — wisdom-engine.ts（Situation Classifier: rule-based direct/confirm/plan for bot; Reflection Metrics: firstApproachAccuracy + silenceAccuracy + blindSpot detection）; config.ts 新增 wisdom 區塊（enabled/situationClassifier/reflectionTracking, default off）; index.ts 整合 session_start preload + before_agent_start inject + session_end reflect | Extensions.md |
| 2026-03-14 | **atomic-memory S3**: Session State + Intent + Blind-Spot — session-state.ts（SessionStateManager: Map + 2h TTL 自動清理）; intent-classifier.ts（7 種 intent, 中英文 keyword matching + category boosts）; blind-spot.ts（空結果/弱匹配偵測, inject <13 tokens）; index.ts 整合 session_start hook + before_agent_start intent/blind-spot | Extensions.md |
| 2026-03-14 | **atomic-memory S5**: Cross-session consolidation — cross-session.ts（consolidateNewFacts: 每 fact 查 ChromaDB 找跨 session 重複，自動遞增 confirmations）; promotion.ts 加 immediatePromotionCheck（[臨]→[觀] 即時晉升，[觀]→[固] suggest only）; index.ts agent_end hook 整合 | Extensions.md |
| 2026-03-14 | **atomic-memory S0-S2 (Wave 1)**: S0 index.ts 拆分為模組化入口 + logger/types/forget-engine/context-formatter/workspace-reader 抽出; S1 ACT-R activation scoring + 3 階段 token budget + summary fallback; S2 Ollama 3-stage resilience + write gate CJK operationality | Extensions.md |
| 2026-03-20 | **Evolution Plan Phase 1 調查**: 40 指令×4 等級權限矩陣完成; 發現 3 個關鍵缺口（Core 不 enforce permissionLevel、Discord 無 defaultMemberPermissions、resolveEffectivePermissionLevel 未被 Core 使用）; 多層級權限實作方案（2A Core enforce + 2B Discord visibility + 2C LINE Rich Menu）; 自我迭代可行性分析（路線 A: Plugin Tool+Exec 最可行 → 路線 B: /evolve Skill → 路線 C: CI/CD）; Phase 2-5 拆分 + 每階段 執P prompt | Evolution-Plan.md, _INDEX.md |
| 2026-03-20 | **read-project Phase 1**: 6,350+ 檔案完整掃描 — FileTree.md（47 src 子目錄 + 44 extensions + 174 scripts + 51 skills + 4 apps + root config）、結構映射、差異分析（vs 03-12: +55% TS files、近 50 commits +108K lines） | FileTree.md, _INDEX.md |
| 2026-03-12 | **read-project**: 深度閱讀全專案，建立 10 份文件索引 + Docs-Config-Map | All _AIDocs |
