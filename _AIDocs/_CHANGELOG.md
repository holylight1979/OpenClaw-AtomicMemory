# _AIDocs Changelog

| 日期 | 變更 | 影響文件 |
|------|------|---------|
| 2026-03-21 | **Phase 5 — Guest E2E 驗證 + 整合測試**: (1) g-int.test.ts 新增 3 個 describe 區塊共 22 測試案例 — Guest level resolution（plugin+core 雙層驗證：無 senderId→guest、不在 allowlist→guest、在 allowlist→user）、command gate（hasMinLevel 驗證 guest 可用 help/request-access/whoami/status、不可用 model/allowlist/admin 級指令）、access request flow（submit→duplicate rejection→list pending→approve→deny→re-request→owner notification context）(2) 修正 Scene 3 過期測試（no senderId 由 "user"→"guest" 對齊 Phase 4 Guest 行為）(3) Evolution-Plan.md 搬出 _AIDocs → docs/（規劃文件不符 _AIDocs 收錄標準）| g-int.test.ts, _INDEX.md, Evolution-Plan.md |
| 2026-03-21 | **Phase 4 Guest — Guest 等級實作**: (1) permission-guard.ts resolvePermissionLevel 修正（無 senderId → guest）(2) 新建 access-request.ts 存儲模組 (3) commands-registry.data.ts 新增 4 指令（request-access/approve-access/deny-access/pending-access）(4) index.ts 註冊 4 command handlers + owner pending notification 注入 (5) get-reply.ts guest chat 攔截（不進 LLM pipeline）(6) config.ts guestCommands 設定 (7) plugin-sdk/index.ts 新增 addChannelAllowFromStoreEntry re-export | permission-guard.ts, access-request.ts, config.ts, index.ts, g-int.test.ts, commands-registry.data.ts, get-reply.ts, plugin-sdk/index.ts, Evolution-Plan.md |
| 2026-03-21 | **Phase 4 — /iterate 指令 + 整合收尾**: (1) 新建 self-iterate-tools.ts — 4 核心函數（selfAnalyze/selfPropose/selfApply/selfJournal + recordPitfall）(2) index.ts 註冊 4 個 owner-only 工具（self_analyze/self_propose/self_apply/self_journal）+ /iterate 指令（analyze/propose/apply/journal 子指令）(3) 回饋循環：self_analyze→auto atom_recall、self_apply 成功→auto journal、self_apply 失敗→pitfall atom (4) 權限審計：atom_permission add/remove 自動建立 [permission-audit] event atom (5) g-int.test.ts 補齊 codeModification + systemIdentityPath mock config | self-iterate-tools.ts, index.ts, g-int.test.ts, Evolution-Plan.md |
| 2026-03-21 | **Phase 2B — Discord Command Visibility**: NativeCommandSpec 加 permissionLevel 欄位 + toNativeCommandSpec() 傳入 + createDiscordNativeCommand() 透過 Carbon Command.permission 映射到 Discord defaultMemberPermissions（owner→Administrator, admin→ManageGuild, user/guest→全員可見） | commands-registry.types.ts, commands-registry.ts, native-command.ts, Evolution-Plan.md |
| 2026-03-21 | **Phase 3 Self-Evolution 基礎**: (1) openclaw.json 加入 tools.exec.safeBins 預授權 pnpm/git/tsc + 每個 binary hardened profiles（deniedFlags 擋 --force/--no-verify/--hard）(2) config.ts selfIteration.codeModification 新增子設定（enabled/sourceDir/allowedPaths/blockedPaths/maxFilesPerPass/maxLinesPerPass/requireBuildPass/autoRevertOnFailure）(3) 新建 evolve-guard.ts — 路徑白/黑名單驗證+批次限制+owner-only 權限+journal 日誌+agent context 注入 (4) types.ts 新增 EvolvePathVerdict/EvolveBatchVerdict/EvolvePassStats/EvolveJournalEntry (5) index.ts before_prompt_build hook 整合 evolve guard context 注入（owner+enabled 時自動生效）| openclaw.json, config.ts, evolve-guard.ts, types.ts, index.ts, Evolution-Plan.md |
| 2026-03-21 | **Core-AutoReply-Functions Phase 7 deep read**: src/auto-reply/ + src/cron/ + src/tts/ + src/context-engine/ 共 226 檔 ~44,500 行函式級索引 — 4 大系統：AutoReply（入站 dispatch pipeline、40+ slash command handlers、LLM Agent 執行核心含 model fallback+memory flush+block streaming pipeline、inline directive 解析/持久化、followup queue 系統、model selection 狀態機、session 初始化/fork/reset/usage、reply 路由/threading/投遞、ACP dispatch、platform-specific directives（LINE/Slack/Telegram）、typing indicator、mentions/history/groups）、Cron（CronService facade、timer/jobs 排程引擎、isolated agent turn 執行、delivery dispatch（announce/webhook/direct）、session reaper、run log、schedule 計算、stagger 策略、store 持久化+遷移）、TTS（3-provider fallback（OpenAI/ElevenLabs/Edge）、directive 解析、config 管理、payload 自動套用、文字摘要）、Context Engine（registry pattern、LegacyContextEngine 實作、ContextEngine interface）；完整呼叫關聯圖（入站→dispatch→getReply→directives→agent run→route）+系統歸屬分類 | Core-AutoReply-Functions.md, _INDEX.md |
| 2026-03-21 | **Core-Utilities-Functions Phase 7 deep read**: shared/utils/logging/process/daemon/terminal/tui/wizard/node-host/providers/compat/test-helpers/test-utils 共 227 檔 ~27,323 行函式級索引 — 13 目錄：shared（IP 解析+SSRF 防護+node matching+requirements eval+text processing）、utils（boolean/chunk/fetch-timeout/concurrency/shell-argv）、logging（tslog file logger+console capture+subsystem factory+sensitive redact）、process（command queue multi-lane+process supervisor child/PTY+kill-tree+exec）、daemon（launchd/systemd/schtasks 三平台 service）、terminal（ANSI/table/theme/progress）、tui（gateway WS chat+stream assembler+ink UI+slash commands）、wizard（step machine+clack+gateway config）、node-host（invoke handler+exec policy+allowlist+approval plan）、providers（GitHub Copilot/Kilocode/Qwen/Google）、compat（legacy names）、test-helpers+test-utils（fixtures/mocks/contract suites）；跨模組呼叫圖+系統分類 | Core-Utilities-Functions.md, _INDEX.md |
| 2026-03-21 | **Core-SDK-Functions Phase 6 deep read**: src/plugin-sdk/ + src/memory/ + src/secrets/ + src/security/ 共 198 檔 ~35,182 行函式級索引 — 4 大子系統：Plugin SDK（87 檔，channel lifecycle/webhook targets+guards/command-auth/group-access/inbound envelope+reply/persistent dedupe/file-lock/ssrf-policy/windows-spawn + ~30 channel-specific barrel re-exports）、Memory（58 檔，MemoryIndexManager 核心/6 embedding providers openai+gemini+voyage+mistral+ollama+local/hybrid search vector+FTS/MMR rerank/temporal-decay/query-expansion/batch embedding pipeline/QMD backend/multimodal）、Secrets（33 檔，runtime snapshot lifecycle/secret ref resolve 3-layer/target-registry 5 檔 pattern 匹配/apply+audit+configure commands/config collectors channels+core+tts+auth）、Security（20 檔，runSecurityAudit 主入口+25 sync+async 檢查項/sensitive-filter 三層過濾 Phase 2.5/external-content injection 防護+boundary wrapping/DM policy shared/fix chmod+icacls/skill-scanner/Windows ACL）；跨模組依賴：plugin-sdk/command-auth→security/dm-policy-shared、secrets/resolve→security/audit-fs+scan-paths、memory/secret-input→secrets/resolve-secret-input-string | Core-SDK-Functions.md, _INDEX.md |
| 2026-03-21 | **Core-Media-Functions Phase 4 deep read**: src/browser/ + src/media-understanding/ + src/media/ + src/link-understanding/ + src/markdown/ + src/canvas-host/ 共 169 檔 ~26,631 行函式級索引 — 6 大子系統：Browser（Server/Route/Playwright engine/Chrome MCP engine/CDP 層/Client API/Extension Relay/Bridge/Config+Auth+SSRF）、Media Understanding（10 providers/runner/apply/attachments/audio transcription）、Media（server/store/fetch/image-ops/MIME/PDF extract/ffmpeg）、Link Understanding（detect→run→format pipeline）、Markdown（IR parser 973 行/render/tables/WhatsApp）、Canvas Host（HTTP+WS server/A2UI/file resolver）；跨模組依賴圖：browser→media(store+image-ops)、link→media-understanding(scope+resolve)、media→markdown(fences)、canvas→media(mime) | Core-Media-Functions.md, _INDEX.md |
| 2026-03-21 | **Phase 2C — LINE Rich Menu 權限分層**: 不同權限等級用戶在 LINE 看到不同 Rich Menu — (1) config-schema.ts 加 richMenus schema (2) 新建 rich-menu-binding.ts 綁定服務（resolveEffectivePermissionLevel → per-user bind + 1hr cache）(3) bot-handlers.ts 3 個 webhook handler 整合 fire-and-forget binding (4) 新建 scripts/setup-line-rich-menus.ts 一鍵建立 owner/admin/user 三層 menu | config-schema.ts, rich-menu-binding.ts, bot-handlers.ts, setup-line-rich-menus.ts, Evolution-Plan.md |
| 2026-03-21 | **Core-Commands-Functions Phase 3 deep read**: src/commands/ 235 檔 ~43,510 行函式級索引 — 21 子系統（Agent Execution/Auth Choice 15+ providers/Channels/Configure/Doctor 20+ 子模組/Gateway Status/Health/Models/Onboarding/Provider Setup/Sessions/Status/Backup/Sandbox/Reset/Setup/Dashboard）、~40 slash command handlers 完整對照、呼叫圖 | Core-Commands-Functions.md, _INDEX.md |
| 2026-03-21 | **Core-CLI-Functions Phase 3 deep read**: src/cli/ 182 檔 ~27,600 行函式級索引 — 18 子目錄、Bootstrap/Commander/Route/Gateway/Daemon/Browser/Nodes/Cron/Update 20+ 分類、啟動鏈+Command 執行鏈+Gateway RPC 鏈+Secret Resolution 鏈呼叫圖 | Core-CLI-Functions.md, _INDEX.md |
| 2026-03-21 | **Core-Hooks-Functions Phase 3 deep read**: src/hooks/ 25 檔 4,333 行函式級索引 — 核心事件引擎/Hook 探索與設定/4 bundled handlers/Gmail 子系統/訊息 Hook 橋接/安裝子系統、呼叫圖 | Core-Hooks-Functions.md, _INDEX.md |
| 2026-03-21 | **Core-Config-Functions Phase 3 deep read**: src/config/ 136 檔 27,851 行函式級索引 — I/O 核心/Zod Schema 14 sub-schema/驗證/預設值/路徑解析/環境變數/$include/merge-patch/Legacy Migration 27 rules/Redaction/Session Store 16 檔/Policy、呼叫圖+18 系統分類 | Core-Config-Functions.md, _INDEX.md |
| 2026-03-21 | **Core-Plugins-Functions Phase 3 deep read**: src/plugins/ 55 檔 9,573 行函式級索引 — Loader(Jiti+LRU cache)/Discovery(FS 掃描+安全檢查)/Manifest(Zod schema)/Registry(createApi+register*)/Hooks(24 lifecycle hooks+runner)/Runtime(12 模組 facade)/Install(npm/archive/dir/file)/Update(channel sync)/Commands(plugin 指令+auth)/Provider(auth wizard)/Tools(optional allowlist)、完整呼叫圖+系統分類 | Core-Plugins-Functions.md, _INDEX.md |
| 2026-03-20 | **Phase 2.5 敏感資訊過濾（安全核心）**: 三層程式碼級防線防止敏感資訊洩漏 — (1) before_tool_call 攔截：敏感路徑 glob 匹配，非 Owner 靜默阻止工具執行 (2) tool_result_persist 過濾：掃描工具結果內容模式（API key/token/password/private key），非 Owner 清空 / Owner 只告知路徑 (3) before_message_write 最後防線：LLM 輸出掃描替換敏感段落為 `[已過濾]`；核心層實作在 plugin hooks 之前執行，不可被 prompt injection 繞過；SecurityConfig 支援 config 自訂 patterns/paths 擴展 | sensitive-filter.ts, types.security.ts, types.openclaw.ts, pi-tools.before-tool-call.ts, pi-tools.ts, session-tool-result-guard.ts, session-tool-result-guard-wrapper.ts, attempt.ts |
| 2026-03-20 | **Core-Agents-Functions deep read**: src/agents/ ~408 檔函式級索引 — 13 子系統（runner/tools/sandbox/auth-profiles/skills/subagent/models-config 等）、完整 export 簽名+型別+說明、Agent Lifecycle 關鍵路徑、Tool 系統（20+ tool factories + 7 層包裝管線）、跨 25+ 模組依賴 | Core-Agents-Functions.md, _INDEX.md |
| 2026-03-20 | **Core-Gateway-Functions deep read**: src/gateway/ ~180 檔函式級索引 — 全 export 簽名+型別+說明、106 RPC methods（agent/chat/config/cron/nodes/sessions 等 handler groups）、14 HTTP routes（hooks/openai/openresponses/canvas/plugins/control-ui）、18 WS events、16 子系統分類、跨 20+ 模組依賴（infra ~127/config ~121/agents ~53）、auth 多模式+rate limiting、protocol v3 schema、channel manager lifecycle、node registry | Core-Gateway-Functions.md, _INDEX.md |
| 2026-03-20 | **Phase 2A identity 補完**: command-auth.ts 加 getCachedSystemIdentity() 傳入 identity → admin 用戶在 core 層可被正確識別; native-command.ts 改用 await loadSystemIdentity(); permission-level.ts 新增 getCachedSystemIdentity() sync getter; plugin-sdk re-export 更新 | permission-level.ts, command-auth.ts, native-command.ts, plugin-sdk/permission-level.ts |
| 2026-03-20 | **進化計畫 Phase 1+2 — 統一權限等級 + 指令權限閘門**: Phase 1: (1) src/channels/permission-level.ts 新建（PermissionLevel 型別 owner/admin/user/guest、數值系統 hasMinLevel()、resolveEffectivePermissionLevel() 統一解析）(2) plugin-sdk re-export 層 + package.json export (3) 全部 60+ 指令標註 permissionLevel (4) CommandAuthorization 加 senderPermissionLevel (5) permission-guard.ts re-export 重構消除重複; Phase 2: (1) handleCommands() 統一閘門 — findCommandByTextAlias + hasMinLevel check (2) Discord dispatchDiscordCommandInteraction() ephemeral 拒絕 (3) /commands 列表按 sender level 過濾 + [owner]/[admin] 標籤; 16 files +346 -154 lines | permission-level.ts, command-auth.ts, commands-registry.data.ts, commands-core.ts, native-command.ts, status.ts, permission-guard.ts |
| 2026-03-18 | **Owner Challenge Auth 修正 + 使用者記憶刪除權限放寬**: (1) Fix Escalation 調查：確認根因為 jiti cache 陳舊（plugin .ts 修改後未清 cache 導致舊編譯結果持續生效）; (2) challenge flow 重排修正保留：challenge 在 command interception 之前執行 + authIntentHandled flag 豁免; (3) Ollama 移除：auth intent 偵測不再依賴 qwen3（純 keyword regex）; (4) 使用者記憶刪除權限：移除 [臨] confidence 限制，改為 sources.senderId 比對（使用者可刪除自己建立的任何記憶）; (5) run.ts hookCtx 已有 sender fields（lines 331-335），dist/ 已包含; (6) Discord owner 認證 E2E 驗證通過（senderId=831783571513278464, hex challenge + 成功註冊） | index.ts, _CHANGELOG.md |
| 2026-03-16 | **Owner 認證機制 + MEMORY.md 動態索引**: (1) owner-challenge.ts 新建 — hex challenge 生成/MD5 驗證/24hr cooldown/Ollama 90% 意圖確認; (2) memory-index.ts 新建 — MEMORY.md 程式碼自動管理，+4/-1 scoring（cap 20），每次對話更新，score=0 移除; (3) permission-guard.ts 新增 registerOwnerPlatform() + authenticatedAt; (4) index.ts: challenge 流程接線（before_agent_start 意圖偵測→challenge→驗證→註冊）+ MEMORY.md scoring 獨立於 capture（agent_end 頂部）; (5) E2E 驗證：MEMORY.md scoring 累加正常（4→8）、新 atom 正確加入、workspace 碎片已排除 | owner-challenge.ts, memory-index.ts, permission-guard.ts, index.ts |
| 2026-03-15 | **記憶+身份統一整合**: (1) System.Owner.json 集中式身份 Registry — 統一 owner/bot/admin 跨平台身份定義（取代 6+ 散布位置），permission-guard.ts 平台感知 owner 匹配 + 自動 fallback; (2) Recall 統一搜尋 Phase 11 — recall-engine.ts 加入 workspace 日期檔 keyword 搜尋，workspace facts 包裝為 pseudo-atom（score×0.35 低權重）混合排序，去重避免與既有 atom 重複; (3) /whoami 指令 — before_agent_start 偵測 whoami 意圖回傳 senderId+channel+permission; (4) Workspace 增強 — SOUL.md 加入讀取清單 + context-formatter source 標記; (5) config.ts systemIdentityPath 設定 | permission-guard.ts, recall-engine.ts, workspace-reader.ts, context-formatter.ts, types.ts, config.ts, index.ts, System.Owner.json |
| 2026-03-15 | **atomic-memory G-INT**: 三目標整合驗證 — 靜態驗證（TSC 零錯誤、hook 順序、config 三區段共存、無 runtime 循環依賴）; 整合測試 33 cases 全 PASS（5 場景：跨群組 scope routing + 跨平台 identityLinks entity resolution + 權限三級攔截 + G1+G2+G3 交叉 + bot self-awareness）; live gateway 測試（store/recall/self-awareness 全通過）; 衝突檢查（crossPlatform vs memoryIsolation 獨立、scope vs sources 正交、defaults 零設定可用） | Extensions.md |
| 2026-03-15 | **記憶+身份統一整合**: System.Owner.json 集中式身份 Registry + Recall 統一搜尋 Phase 11 + /whoami 指令 + Workspace 增強 | permission-guard.ts, recall-engine.ts, config.ts, index.ts |
| 2026-03-15 | **atomic-memory G-INT → S0-S8 → G1-G3**: 模組化重構 + ACT-R scoring + Ollama resilience + session state + episodic/wisdom/self-iteration engines + 跨群組 scope + 跨平台 identity + 權限認知 + 整合驗證 33 cases | Extensions.md |
| 2026-03-14 | **atomic-memory S3-S5**: Session State + Intent + Blind-Spot + Cross-session consolidation + promotion | Extensions.md |
| 2026-03-20 | **Core-ACP-Functions Phase 2 deep read**: src/acp/ 36 檔 6,500+ 行函式級索引 | Core-ACP-Functions.md, _INDEX.md |
| 2026-03-20 | **Evolution Plan Phase 1 調查**: 40 指令權限矩陣 + 多層級方案 + 自我迭代路線圖 | Evolution-Plan.md, _INDEX.md |
| 2026-03-20 | **read-project Phase 1**: 6,350+ 檔案完整掃描 — FileTree.md | FileTree.md, _INDEX.md |
| 2026-03-12 | **read-project**: 深度閱讀全專案，建立 10 份文件索引 + Docs-Config-Map | All _AIDocs |
