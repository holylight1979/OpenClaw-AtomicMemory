// Narrow plugin-sdk surface for the bundled atomic-memory plugin.
// Keep this list additive and scoped to symbols used under extensions/atomic-memory.

export type {
  OpenClawPluginApi,
  OpenClawPluginToolFactory,
  OpenClawPluginToolContext,
} from "../plugins/types.js";
